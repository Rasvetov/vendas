package project.rasvetov.repositories;

import org.springframework.data.jpa.repository.JpaRepository;
// Classe responsável por acessar o banco de dados diretamente para fazer operações para a classe itemPedido
public interface ItemPedidoRepository extends JpaRepository {
}
